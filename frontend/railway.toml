# Docs only — set in Railway dashboard for the Web service:
#   Dockerfile path = frontend/Dockerfile
#   Domain = revisit.argusai.online

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
