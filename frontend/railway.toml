# Railway: set Root Directory to `frontend`
# Build uses Dockerfile; INTERNAL_API_URL must point at the API service.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
