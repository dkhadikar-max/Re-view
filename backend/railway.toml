# Docs only — do not rely on this for multi-service Railway.
# Set Dockerfile path in the Railway dashboard per service:
#   API → backend/Dockerfile
#   Web → frontend/Dockerfile
#
# A root railway.toml would force ONE dockerfile onto every service.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
